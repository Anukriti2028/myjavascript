"use strict"; // treat all JS code as newer version
/*
The javascript engine is hidden inside the browser but we are
not writting our code on browser , we are writting on the
node.js so alert(3+3) will give an error but if we run the same alert statement on console 
it will print 6.
*/

let name ="hitesh"
let age = 18
let isLoggedIn = false
console.log(typeof age);
console.log(typeof null); //object
console.log(typeof undefined); //undefined

// Primitive data types : number , bignint , string , boolean(true,false),null(standalone value), undefined,symbol(unique)
//Non-primitive data types : objects
/*

 */
/*
undefined means you have not defined the value , we have only declared the 
variable and null is the representation of empty value means 
we are intentionally saying that is it empty.
*/


